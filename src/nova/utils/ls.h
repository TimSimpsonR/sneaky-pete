#ifndef _NOVA_UTILS_LS
#define _NOVA_UTILS_LS

#include <string>
#include <vector>

namespace nova { namespace utils {

    void ls(const std::string & directory, std::vector<std::string> & output);

} }

#endif
